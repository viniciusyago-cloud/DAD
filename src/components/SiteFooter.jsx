import React from "react";

/* Site-wide credit line — shown on every page. */
export default function SiteFooter() {
  return (
    <footer className="site-foot">
      Created by <b>Salles</b> <span>(GOAT &amp; diva)</span>
    </footer>
  );
}
